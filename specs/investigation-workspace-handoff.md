# Promote the investigation prototype

Prepared 2026-09-11; prototype approved by the user on 2026-09-12. **Prototype complete; ready for production implementation after the relevant broker questions below are answered.** This handoff does not authorize a release. No planning skills were used.

## Starting point

- Implementation baseline: `8575a2c` on `prototype/investigation-workspace` (approved prototype). Earlier milestones: `a908ffd` for persisted Watch/preferences and typed deletion; `8cc1232` for profile themes, warnings and new profiles. Use the normal checkout and inspect newer commits before starting; preserve unrelated changes.
- Visual/interaction reference: [prototype](../tools/ServiceBusEmulatorExplorer.InvestigationPrototype/README.md) and its [current preview](../tools/ServiceBusEmulatorExplorer.InvestigationPrototype/preview.png). Apply [UI language and audit corrections](ui-language.md) consistently; preserve the approved layout.
- Destination: existing WPF [App](../src/ServiceBusEmulatorExplorer.App/ServiceBusEmulatorExplorer.App.csproj) using existing [Core contracts](../src/ServiceBusEmulatorExplorer.Core/ServiceBus/ServiceBusContracts.cs). Keep the prototype as a reference until parity is proven. Do not just rename its executable or ship its synthetic Workspace.
- Current evidence: 347 routed walkthrough checks and an 11-check real tray run pass, including profile/Watch preferences, typed deletion and shared styling. Full-profile themes, saved warnings, new profiles and simplified Watch search pass the current rendered walkthrough. No new broker integration tests were run for this audit. The proof uses synthetic messages and is not a production readiness certificate.

## Approved prototype milestone

- [x] User approved the prototype as ready; the layout and implemented interactions are the design reference.
- [x] Final toolbar approved: no accent line; retain only the neutral bottom divider. Conventional Settings cog, no duplicate Settings heading, shared rounded copy icons beside masked connection fields, and themed scrollbars are part of the final design.
- [x] Shared styling, complete profile themes, clear-red Delete, compact layouts and simplified Watch search implemented.
- [x] Toolbar connection selector, profile creation/editing, optional warnings and off-by-default auto-connect-on-switch implemented. The duplicate General-settings connection picker is removed.
- [x] Preferences persist, including per-profile Watch inclusions and connection warnings; credentials use Windows user-scoped protection.
- [x] Active/DLQ deletion requires exact `DELETE`; warning cancellation preserves the approved connection and pending saves cannot select an unapproved profile.
- [x] Rendered walkthrough, restart/persistence and tray evidence recorded in the [prototype report](../tools/ServiceBusEmulatorExplorer.InvestigationPrototype/verification.md) and [tray report](../tools/ServiceBusEmulatorExplorer.InvestigationPrototype/tray-verification.md).
- [ ] Production workflows implemented and proven against real brokers (separate completion checklist below).

Do not reopen visual design, add redundant controls, or run another prototype-design phase by default. Transfer the approved interaction contract to the existing App. Physical keyboard, screen-reader and DPI/multi-monitor validation still belongs to production verification; approval does not turn missing evidence into a pass.

## Requirements mapped to what exists

“Prototype” means the interaction exists, not that the broker behavior exists.

| Conversation requirement | Current state | Promotion work |
| --- | --- | --- |
| Small daily-investigation UI; remove redundant headings | Approved layout implemented | Replace old shell surface; do not bring back all old administration buttons |
| Emulator and Azure connection selection | Toolbar dropdown with opt-in auto-connect; Settings manages profiles; Add connection opens a blank uniquely named profile without switching; Save persists | Reuse real profile/auth validation; persist selected profile and color separately from connection health |
| Share connection strings | Copy beside each masked runtime/administration field copies its current value; empty fields disable Copy; status never includes credentials | Preserve explicit clipboard sharing and masking; never log credentials |
| Profile identity and connection health | Active profile theme covers buttons, selected states and surface tints across windows; separate fixed health, DLQ and red Delete colors | Bind health to structured connection/operation outcomes; color alone must not communicate status |
| Queues, topics, subscriptions and Message/DLQ totals | Sample tree, distinct icons; unknown sample counts | Bind real tree and typed known/unknown/stale count state; topic totals are delivery totals across subscriptions |
| First 50 on scope/bucket change; Load more | Implemented against fixtures | Per-source cursors, cancellation, stale-request rejection; settle topic aggregate paging |
| Independent checkboxes; aligned select-all | Implemented | Stable source-aware row identity; select-all means displayed results, not unseen broker messages |
| Refresh/auto-refresh/pause without losing work | UI implemented; refresh can inject samples | Read-only, single-flight refresh preserving focus, selection, drafts and scroll; no simulated arrivals |
| One left search with entity/ID suggestions | Implemented | Real entity index plus loaded/recent IDs; never imply suggestions enumerate unseen broker messages |
| Global correlation/message ID search, OR and * | Implemented over complete memory snapshot | Cancellable paged peeking across sources; bounded scan and truthful incomplete status |
| Exact case-sensitive IDs; OR keyword ignores case; quoted literals | Implemented, including explicit correlation:/message: clauses | Preserve grammar and tests; do not add ? or implicit substring ID matching without approval |
| Search tree shows only matching branches and match counts | Implemented; totals retained separately | Project discovered matches, aggregate parents, restore authoritative totals on Clear |
| Location/State in global search and topic views | Implemented; hidden when browsing one queue/subscription | Preserve source identity even when the column is hidden |
| Clear removes applied filter and text | Implemented | Cancel old scans and restore browse scope; old callbacks must not repopulate results |
| Preview JSON, Properties, Raw; always wrap | Implemented in that order | Preserve bytes/metadata; label malformed/non-JSON honestly; no wrap toggle |
| Inline formatted/colorized edit, undo, discard | DLQ editor implemented | Preserve drafts by delivery identity; validate before send; define connection-switch behavior |
| Replay by default; Edit and Replay after changing JSON | Implemented with synthetic copies | Real send, batch eligibility and per-item outcomes; original DLQ remains unless separately deleted |
| Default IDs original-replay-N | Session-only counter and synthetic collision check | Persist or otherwise define counter/uniqueness; preserve metadata; never promise global retry count |
| Delete Active and DLQ messages, focused or checked batch | Confirmation lists Active/DLQ counts; exact uppercase `DELETE` enables the action; Cancel is the default | Preserve the typed gate; implement source-aware completion with per-item results, vanished/locked-message handling and cancellation; no purge or implicit unseen-message delete |
| Copy icon consistent and near correlation | Selected rounded icon everywhere | Share vector resource; copy exact ID/body/properties, preserve action labels and tooltips |
| Watch Active/DLQ independently at connection, topic or leaf scope | Global rules include future entities; topic rules include future subscriptions; explicit bucket overrides and a searchable inclusion tree; 15s fake-arrival timer | Refresh discovery before resolving effective sources; preserve category/topic/child inclusion precedence; detect arrivals without consuming messages; baseline, dedupe, reconnect and DLQ cursor policy |
| Persistent desktop alert; Investigate restores app | Separate topmost WPF window, real tray | Keep until response; position on correct monitor; no transient-toast substitution |
| Investigate adds to current cases | Implemented OR union, dedupe, mixed fields, focuses newest case | Preserve applied criteria; cancel superseded scan safely; handle expired/unavailable message |
| Close-to-tray configurable; explicit Exit | Real prototype lifetime behavior; preferences stored locally | Integrate app shutdown and cancellation; production settings migration; no invisible orphan process |
| Full-width activity console; clear only expanded | Implemented with 100 in-memory entries | Structured outcomes, redacted sensitive values, bounded history; decide persistence |
| UTC/Local/Server selector | Display preference persisted; Server is sample −05:00 | Configure real server zone; retain original UTC instants and raw payloads |
| Restore selections/preferences across launches | Separate prototype JSON store; user-scoped DPAPI for connection strings; per-profile Watch snapshots | Integrate production settings safely; migration, failure recovery and restart behavior must be proven |
| Consistent colors/fonts/sizes/states | Shared resources, themed scrollbars, conventional cog, a neutral gray bottom toolbar divider and Settings without a duplicate content title implemented; rendered desktop/compact/minimum checks pass | Preserve approved tokens and complete the linked UI audit before production handoff |

Keep entity creation/edit/delete, purge, advanced rule administration, scheduling and unrelated legacy features outside the primary workflow. Retaining existing backend code is different from exposing its entire old UI. Standalone Send New Message and replay of Active messages were not settled; do not silently add them.

## Questions before implementation

### Accepted first-slice decisions (user answers, 2026-09-12)

- **Q2:** Messages means Active; add a third **Scheduled** count column where broker information is available, alongside DLQ. Queue and topic SDK runtime properties expose scheduled counts; subscriptions do not, so show an explicit unavailable/not-applicable marker rather than invented zero. The new column is an approved addition to prototype parity.
- **Q7a:** Logs and message drafts remain in memory for the session only; profiles, user preferences and Watch rules still persist as already approved.
- **Q7b:** Retain both connection-string and explicit Azure CLI authentication choices.
- **Q8:** Switching with dirty drafts offers Cancel or explicit Discard; an approved switch cancels old reads and isolates connection caches. Do not silently discard drafts.
- **Q9:** Remove Server timezone entirely. Only UTC and computer Local display remain; original timestamp instants and raw metadata stay unchanged.
- **Q10:** Topic page size is a combined delivery count. User clarified **three defaults in Settings: Queues, Topics, Subscriptions**, each defaulting to 50 with selector choices 25/50/100/200. Larger page sizes may use multiple SDK requests. These are type-wide defaults, not overrides for every named entity.
- **Q3:** User approved a default search budget of 30 seconds or 10,000 scanned deliveries, whichever occurs first. Limits are configurable in Settings; partial status and Continue must preserve scan cursors.
- **State badges:** User approved the generated badge proposal during integration: Active uses pale blue `#EFF6FF`, a one-pixel `#6C9BD2` border, and dark blue `#174A7E` text, retaining compact rounded geometry. Apply to selected/unselected list rows and the inspector badge.
- These answers supersede the earlier unanswered-question records and corresponding prototype behavior. Q4–Q6 remain unresolved for their later stages; do not reopen accepted choices.

Current ownership: coordinator owns shared Core investigation contracts, shell/view models, DI and this handoff. `preferences` owns protected storage supporting files under App `Investigation/Settings/` excluding `SettingsWindow*`, plus `InvestigationPreferencesTests.cs`; `settings_ui` owns only `SettingsWindow.xaml/.cs`; `discovery` owns new Core `InvestigationEntityBrowser.cs`/`Discovery*` and `InvestigationDiscoveryTests.cs`; `paging` owns `DeliveryPager.cs`/`Paging*` and `InvestigationPagingTests.cs`; `drafts` owns `DeliveryInspector.cs` and `InvestigationDraftTests.cs`. Workers use Luna High, have no runtime leases, and do not modify shared contracts. Coordinator serializes builds, UI and emulator runs. Previous leaf proof milestone: `f1639d0`.

Answer by Q number. Recommendations are proposals, not already approved decisions. Existing approved interaction details above do not need to be asked again.

| ID | Decision needed | Recommended starting choice |
| --- | --- | --- |
| Q2 | Should Messages mean Active or total including DLQ/scheduled? | Active and DLQ separately, explicit tooltip; preserve unavailable counts. Never substitute a loaded-page length. |
| Q3 | Search limits and completeness: how much work may one search do? | Both Active+DLQ in selected connection; configurable time/message budget with Stop/Continue and visible partial status. Agree actual limits before coding. |
| Q4 | Watch interval, baseline, reconnect and notification backlog? | No initial-backlog alerts; configurable polling; retain distinct pending cases until response. Agree interval, cap and restart policy; 15s is only the mock default. |
| Q5 | Accept subscription replay publishing to its parent topic, potentially reaching other subscriptions? | Make destination and routing consequence clear before sending. A subscription is not a direct send destination. |
| Q6 | Replay numbering across sessions/users and ambiguous send failures? | Keep readable replay suffix plus guaranteed unique send identity; define persistence/ID format and retry policy. Do not auto-retry an uncertain successful send. |
| Q7 | Production log/draft persistence and Azure CLI auth visibility? | Profiles, selected connection, preferences and Watch selections are approved to persist with Windows user-scoped secret protection. Decide whether logs/drafts persist and how Azure CLI auth appears; do not reopen the settled preference-storage decision. |
| Q8 | Switching connection with dirty drafts/active work? | Warn about unsaved edits, cancel old operations, isolate all connection caches. Never silently discard production drafts. |
| Q9 | What is “Server” timezone? | Explicit per-profile Windows time-zone ID with DST rules; do not infer broker timezone or ship fixed −05:00. |
| Q10 | Topic view pagination: 50 total or 50 per subscription? | 50 combined deliveries with source identity, matching the simple UI; per-source cursors required. |

Q1 (delete scope) and Q11 (layout/density) are settled: retain typed confirmation for Active and DLQ, and preserve the approved compact layout. Do not add automatic console collapsing during promotion. Auto-connect, profile warnings, persistence and Watch inclusion semantics are also settled; only their broker implementation needs design work.

Resolve questions in small groups before the dependent work. The recommendations in this document are not consent:

| Work to start | Questions needed first |
| --- | --- |
| Profile/settings and read-only browsing | Q2 count meaning; Q7 authentication and persisted data; Q8 switching with work in progress; Q9 configured time zone; Q10 topic pagination |
| Global search | Q3 scan budgets and completeness |
| Watch and desktop notifications | Q4 polling, baseline, backlog and restart policy |
| Replay | Q5 topic routing and Q6 IDs/retries |

Independent resource migration and read-only repository investigation may continue while answers are pending. Do not invent product answers or block every worker on a question affecting only one feature. Ask only genuinely unresolved choices; implementation details such as focused class names, cancellation plumbing and atomic file writes belong to the coordinator.

Settled scope: delete supports Active and DLQ, focused or checked messages, with explicit typed `DELETE` confirmation. Connection selection uses the toolbar dropdown; Settings retains profile management and an off-by-default, saved auto-connect-on-switch option. Warning approval remains required before automatic connection; a saved profile color themes buttons, selection and surfaces across windows while semantic health, DLQ and red Delete colors retain their meanings. Add connection creates an empty editable profile without switching. An optional saved warning requires confirmation before switching, Connect or restored startup connection attempts; Cancel preserves the old connection or blocks the pending attempt. Watch search uses an inline magnifier and concise placeholder. Watch supports global and topic rules, including subsequently discovered entities, with a searchable inclusion tree and mixed parent states. A branch choice applies to descendants, and more specific child choices override inherited inclusion. Profiles and user selections/preferences persist across launches; Watch rules belong to a profile. These interaction decisions do not settle real polling or broker mutation algorithms above.

## Reuse and the traps to avoid

- [Message service](../src/ServiceBusEmulatorExplorer.Core/ServiceBus/ServiceBusMessageService.cs) already peeks non-destructively with `take`, `fromSequenceNumber` and cancellation. [MessageInspectionViewModel](../src/ServiceBusEmulatorExplorer.App/ViewModels/MessageInspectionViewModel.cs) already contains 50-row paging and stale-result defenses, but its refresh/selection behavior differs from the prototype. Extract focused workflows rather than adding every feature to this large class.
- [Administration service](../src/ServiceBusEmulatorExplorer.Core/ServiceBus/ServiceBusAdministrationService.cs) and [tree builder](../src/ServiceBusEmulatorExplorer.Core/ServiceBus/EntityTreeBuilder.cs) are reusable. Counts currently use non-null numeric fields, so unknown cannot yet be represented correctly.
- Topic Active/DLQ counts are initially constructed as zero in the administration adapter, then `EntityTreeBuilder.AggregateTopicCounts` sums subscription counts before returning discovery. Preserve this existing aggregation; add explicit unavailable/partial states so failed or incomplete discovery cannot become an authoritative empty topic. This detail was rechecked during production intake on 2026-09-12.
- [Client factory](../src/ServiceBusEmulatorExplorer.Core/ServiceBus/DirectServiceBusClientFactory.cs) validates the administration connection during Connect but does not prove runtime connectivity. A green Connected label must not imply both paths are healthy solely because administration discovery succeeded. Define truthful readiness and non-destructive validation with the connection worker.
- Historical [emulator evidence](codebase-audit-remediation-plan.md): on 2026-07-10, 60 messages were sent and a 50-message page was peeked while runtime ActiveMessageCount remained zero for three minutes. This is a recorded limitation, not a fresh emulator retest. Do not block discovery/search on reported zero or wait indefinitely for the emulator to fix counts.
- [Replay service](../src/ServiceBusEmulatorExplorer.Core/ServiceBus/DeadLetterReplayService.cs) and [request factory](../src/ServiceBusEmulatorExplorer.Core/ServiceBus/DeadLetterReplayRequestFactory.cs) support safe copy and separate DLQ deletion. Subscription replay maps to parent topic. The prototype's direct fan-out into child fixture lists is not real routing.
- Existing replay uses the NewGuid ID policy and existing deletion supports DLQ only. They are useful foundations, not parity with readable replay suffixes or approved Active deletion. Implement those gaps deliberately after the relevant contracts and questions are settled.
- Define and test the sendable metadata to preserve: the current mapper copies body, content type, correlation/session IDs, subject and application properties, but does not preserve every property (for example ReplyTo, To, PartitionKey or TTL). Broker-generated metadata remains observational, not replay input.
- [Profile store](../src/ServiceBusEmulatorExplorer.Core/Connection/JsonConnectionProfileStore.cs) supports saved profiles and Azure CLI formats, but connection strings are serialized and saves use direct file creation. Masked fields are not encryption; general settings need safe atomic persistence and migration.
- Global scan results are observations over time, not a transactional namespace snapshot. Peeking must not receive/lock/complete messages. Messages can disappear while a user inspects them.
- A simple increasing DLQ sequence watermark can miss an older message entering DLQ later. Prove a baseline/rescan strategy against the actual emulator and Azure before declaring Watch reliable. Counts alone cannot detect arrivals when counts are unknown or unchanged.
- Use identity `(connection generation, entity address, bucket, sequence number)` for a delivery. MessageId and CorrelationId are not unique row keys. Group batch mutations by actual source and expose partial outcomes.

## Implementation sequence: small stages, many Luna High workers

Use `gpt-5.6-luna` with reasoning effort `high` for workers. The main agent coordinates and reviews. No scheduled task is created by this document.

1. **Resolve questions and freeze contracts.** Coordinator records answers here, confirms selected branch and clean baseline, and defines delivery identity, count availability, scan progress, watch observations and mutation outcomes. Use the committed reference states. Ask only questions required by the next slice; leave unrelated stages pending. No worker invents conflicting shared contracts.
2. **Promote one usable read-only slice.** Shared themed resources + shell, real Settings/connect with profile creation, protected persistence, optional warnings and opt-in auto-connect; tree, first page, inspector, selection and Clear. Preserve cancellation before profile commitment and restore browsing after an approved reconnect. Keep old services and their tests. Prove this slice before adding mutations.
3. **Parallel feature work after contracts exist.** Suggested ownership below; coordinator owns shared shell wiring/DI and integration. Workers do not all modify ShellViewModel/MainWindow.
4. **Integrate and prove each feature.** Browse → global search → Watch/tray → replay/delete. Test stale results, cancellation, connection changes and partial failures at every boundary. Remove all simulation from the shipping composition.
5. **Final visual and broker gates.** Normalize remaining UI states, run real emulator proof, then authorized Azure proof if available. Commit completed milestones. Do not merge, tag or publish as a side effect of finishing this handoff.

| Luna High worker | Bounded ownership | Evidence required |
| --- | --- | --- |
| A: UI resources | New resource dictionaries/vector assets; coordinator integrates windows | All windows share tokens; contrast/focus/compact screenshots |
| B: Connection/settings | Profile/settings services and their tests | Safe persistence/migration, add/edit profiles, warnings before commitment, opt-in auto-connect, validation, configured time zones, secret redaction |
| C: Browse/counts | Entity/count projection and paging workflow | Unknown counts, source cursors, first 50/load more, stale-session isolation |
| D: Search | Pure matcher and cancellable scan service | OR/*/mixed IDs, partial/empty/errors, per-source bounds; no broker mutations |
| E: Watch | Observation/dedupe scheduler; tray/notification workflow in separate files | Baseline/reconnect/old-sequence DLQ arrivals, single-flight polling, persistent alerts |
| F: Message actions | Replay ID policy, batch outcomes, approved delete workflow | Original preserved, correct destination, partial/uncertain outcomes, confirmations |

Run up to six implementation workers concurrently when dependencies allow; use remaining slots for independent review, never duplicate ownership. Workers must request the coordinator's lease before Docker, ports, tests with shared runtime, databases, or external services. The coordinator serializes those operations and integrates patches. Pure code reads and isolated unit work can overlap.

The coordinator maintains a short execution record in this document: accepted question answers, current slice, exact worker-owned paths, shared contract revisions, active runtime lease, checks/results, milestone commits and next unfinished step. Keep completed prototype status separate from production progress. Freeze shared contracts before dispatching dependent workers. Give each worker the concrete input/output contract, allowed paths, acceptance checks and exclusions. Workers return changes, evidence and remaining issues; they do not independently change shared shell wiring or operate user environments. Coordinate build outputs as shared state too; tests in the same checkout are not automatically isolated.

Review each integrated slice with an independent worker against this requirement matrix and the changed code. The coordinator resolves findings and reruns affected checks before marking the slice complete. Preserve existing useful broker services/tests while replacing the old UI composition; keep ShellViewModel focused on orchestration. Do not copy the prototype's large code-behind or synthetic data model wholesale into production.

Before enabling Active or DLQ Delete, prove that the adapter can target the selected source/sequence safely, including sessions, locks, vanished messages and cancellation. Do not complete unrelated deliveries while scanning for targets. Unsupported broker/entity capabilities must produce an explicit unavailable result, not a fake success or purge fallback. Typed confirmation authorizes only the displayed targets; it does not authorize unattended testing against user messages. Mutation proofs use isolated fixtures on an authorized test environment.

## Production completion checklist (not prototype status)

### Execution record — 2026-09-12

- Active implementation objective established without a token budget. Verified normal checkout on `prototype/investigation-workspace`, clean at `6c16e3d`; this later commit changes the handoff only. Approved code baseline remains `8575a2c`.
- Q2, Q7, Q8, Q9 and Q10 proposals submitted to the user together; **answers pending**, not approved. Q3–Q6 remain for their dependent stages.
- Independent work underway: resource migration, inspector presentation and settled search grammar. Broker and parity audits are read-only. No production slice is complete and no new verification pass is claimed.
- Exact ownership: `resources` owns App `Investigation/Resources/`; `inspector` owns App `Investigation/Inspection/` and `InvestigationInspectionTests.cs`; `search_grammar` owns Core `Investigation/MessageSearchQuery.cs` and `InvestigationMessageSearchQueryTests.cs`. Coordinator owns shared contracts, shell/DI, integration and this handoff. `broker_audit` and `parity_inventory` own no writable files. Workers use Luna High.
- Shared contract: `DeliveryIdentity` contains connection generation, actual receiving address, bucket and sequence number; `MessageDelivery` retains the original `ExplorerMessage`. IDs are not delivery keys.
- Runtime lease: coordinator exclusively ran builds/fast tests, now released. Prototype process 3540 was observed running and left intact. No Docker, broker mutation or production UI-test execution has occurred.
- Next dependent step: record user answers and freeze profile/count/paging contracts, then implement and verify the read-only production slice. Resource-only work may continue meanwhile.

#### Foundation milestone evidence

- Added reusable approved style/icon dictionaries (including distinct Global Watch and Discard vectors), the pinned prototype AvalonEdit version, focused inspection presentation/colorization, settled ID query grammar, and source-aware delivery contracts. Resources are deliberately not merged into the old shell; the actual investigation shell is still outstanding.
- Core now retains original broker `RawBody` bytes alongside decoded text. An SDK-model-factory test covers invalid UTF-8. Delivery construction checks sequence agreement; tests distinguish sources, buckets and connection generations. Raw-tab integration remains a first-slice requirement, not a completed UI claim.
- Final fast checks: Core **68 passed**, App **75 passed**, zero failed; App WPF build succeeds. Commands used project-level `dotnet test --no-restore -m:1 -nr:false -p:UseSharedCompilation=false --blame-hang-timeout 60s`. Cached App dependency restore succeeded with `NuGetAudit=false`; no vulnerability-audit pass is claimed.
- Initial solution execution spawned excessive MSBuild workers and held output open; only verification workers in the observed launch interval were stopped. Explicit single-worker execution resolved the infrastructure issue. Intermediate missing package restore, date-string assertion, missing test import and empty SDK fixture-body errors were corrected before the final passes.
- Independent foundation reviews: `broker_audit` reviewed contracts/grammar/inspection; `parity_inventory` reviewed resource equivalence and paths. Removed field-ignoring matcher overload and misleading deduplication test, preserved typed metadata, handled non-finite numbers, added token/metadata and delivery tests, retained original bytes, and added missing vector assets. Embedded malformed-quote diagnostics remain identical to the approved prototype; both forms reject input, so the suggested diagnostic redesign was declined. No integrated slice review pass is claimed.
- UI verification is **BLOCKED for production parity**: shipping shell integration and supported-state execution are outstanding. A live reference capture was occluded and excluded from evidence. Committed prototype screenshots remain design authority, not production proof. Emulator, Azure, physical keyboard, screen-reader and DPI/multi-monitor evidence remain uncollected.
- No user answers have arrived for Q2/Q7/Q8/Q9/Q10. Dependent profile/settings/count/paging implementation remains pending those decisions. Q3–Q6 remain unasked for later stages. Foundation milestone committed as `fac677f` (`Add investigation workspace presentation and query foundations`); production completion checklist remains open.

#### Continuation — independent resource completion

- Revalidated clean normal checkout at `0e06d43`. The prior goal turn made concrete foundation progress. No Q2/Q7/Q8/Q9/Q10 answers were present on continuation; their dependent work remains unstarted.
- `profile_theme` (Luna High) owns only App `Investigation/Resources/ProfileTheme.cs` and `InvestigationProfileThemeTests.cs`. Coordinator owns `InvestigationResourceTests.cs`, which exercises compiled component-resource loading, sibling icon resolution and button template instantiation on an STA thread. `parity_inventory` performs read-only review. Only the coordinator holds the build/test lease.
- Compiled-resource check passed after using WPF `Application.LoadComponent` to initialize resource loading in the test host. No application window or broker was launched by this check; it does not prove rendered UI parity.
- Profile-theme migration matches the prototype palette algorithm. Independent resource review found missing named health brushes and a test that injected them; added the approved fixed colors to the shared dictionary and changed the proof to load compiled production resources. Final App suite: **80 passed**, zero failed. Production shell wiring and rendered parity remain outstanding.
- Coordinator started dedicated Compose project `sbe-investigation-proof` using existing pinned images and accepted local configuration, after confirming no Service Bus emulator was running. Other Docker projects are untouched. Administration readiness test: **1 passed**. Coordinator holds the exclusive emulator lease; `peek_evidence` owns only `InvestigationPeekIntegrationTests.cs`, with `broker_audit` reviewing it read-only. The new proof targets generated fixtures only; runtime proof is pending execution.

#### Leaf broker proof — 2026-09-12

- Resource/theme milestone committed as `97ad8be`. Independent final leaf-proof review by `broker_audit`: no actionable blockers in the test's scope. The full workspace is still not implemented; Q2/Q7/Q8/Q9/Q10 remain unanswered.
- New `InvestigationPeekIntegrationTests` run against the dedicated real emulator: **2 passed** in approximately 10 seconds. The tests create 62 messages on a unique queue and 62 topic messages delivered to two unique subscriptions. They exercise leaf pages of 50, cursor advancement, exact IDs/sequence/raw bytes/body size and source metadata, invalid UTF-8 payloads, a canceled peek, and repeated non-consuming Active/DLQ peeks. Queue and first subscription retain 60 Active + 2 DLQ; second subscription retains 59 Active + 3 DLQ, making wrong-source reads detectable.
- Fresh runtime-count evidence: the queue reported Active=0, DLQ=0, Total=0 despite readable 60 Active + 2 DLQ. Both subscriptions reported Active=0, DLQ=0, Total=62 despite their readable Active/DLQ deliveries. These are diagnostic observations, not a reason to skip a source or fabricate a total. This independently reproduces the earlier count limitation.
- Initial compile identified a missing `Azure` import and the absence of `ScheduledMessageCount` on SDK subscription runtime properties. The proof now reports only actual Active/DLQ/Total fields common to the observed entities; no scheduled value is invented. Final command used the integration project, filter `FullyQualifiedName~InvestigationPeekIntegrationTests`, `SBE_RUN_INTEGRATION_TESTS=true`, the documented local emulator endpoints, single-worker MSBuild, detailed output and a 150-second hang bound; each test has its own 120-second operation and 10-second cleanup bounds.
- Tests cleaned up only their generated entities. Coordinator tore down only the dedicated `sbe-investigation-proof` Compose project afterward. No ordinary data, profiles or Docker projects were mutated. No worker holds a runtime lease.
- Remaining evidence: combined topic pagination, production workflows and their emulator E2E, rendered UI parity, Watch/replay/deletion/session/lock behavior, Azure, physical keyboard, screen-reader and DPI/multi-monitor validation. Leaf-service success does not close those checklist items.

- [ ] All questions affecting the current stage answered and recorded; unresolved stages remain unstarted.
- [ ] Approved interaction matrix works in the actual App, not just in the prototype executable.
- [ ] No synthetic fixture injection, fixed server offset, fake connection success or direct subscription fan-out in shipping paths.
- [ ] Fast existing unit/view-model tests remain green; meaningful new tests cover each changed workflow.
- [ ] Emulator proof includes >50 messages, multiple sources, Active+DLQ, zero/unknown runtime counts, cancellation, disconnect/reconnect, replay-copy and confirmed delete if approved.
- [ ] Azure behavior tested when credentials/environment are authorized; otherwise explicitly unverified. Never present emulator-only evidence as Azure certification.
- [ ] Rendered desktop/compact/settings/notification states and keyboard/DPI checks satisfy [UI language](ui-language.md); no user-as-first-tester handoff.
- [ ] Root README remains user-facing; narrow internal docs and affected links updated; `git diff --check` passes.
- [ ] Evidence and remaining limitations recorded; completed work committed on current branch; no release performed.

Start with existing commands in [tests README](../tests/README.md) and inspect scripts before running. Normal fast check: `dotnet test ServiceBusEmulatorExplorer.slnx --filter "TestCategory!=Integration&TestCategory!=UiSmoke"`. Integration/UI runs are opt-in and require the coordinator's runtime lease. Use bounded execution and the repository's UI smoke runner.

## Ready-to-use coordinator prompt

```text
Implement the approved investigation workspace in the real ServiceBusEmulatorExplorer WPF application. Act as the coordinator and use multiple gpt-5.6-luna subagents at high reasoning effort as workers.

Read AGENTS.md, specs/investigation-workspace-handoff.md, specs/ui-language.md and the prototype README, code and reference screenshots. The approved prototype baseline is 8575a2c. Inspect the current checkout and any later commits; do not reset it to that baseline. Work on the current branch in the normal checkout. Do not use planning skills or restart visual design.

First resolve only the open questions required for the next slice, record my answers in the handoff, and continue independent work while waiting. Recommendations are not approved answers. Do not ask again about settled interactions. Freeze shared contracts before dependent workers begin.

Promote the approved UI into src/ServiceBusEmulatorExplorer.App using focused workflows and reusable Core services. Start with one working read-only slice: settings/connections, protected preferences, entity discovery/counts, paging, selection and inspector. Then integrate global search, Watch/tray/notifications, replay and typed-confirmation deletion. Preserve the prototype's exact behavior and styling, including no top accent line and only a neutral bottom toolbar divider. Do not ship synthetic broker behavior or copy the prototype's code-behind wholesale.

Run up to six workers concurrently when dependencies allow, with exact non-overlapping file ownership. You own shared contracts, shell/DI integration and runtime leases. Serialize Docker, shared build outputs, UI tests and external services. Use isolated test profiles and messages; never run destructive proofs against my ordinary data. Have an independent worker review each integrated slice, resolve findings and rerun affected checks.

Keep the handoff current with decisions, worker ownership, progress, evidence and milestone commits. Run meaningful unit, emulator and rendered UI checks; use the UI verification skill. Test Azure only with an authorized environment and report missing evidence honestly. Preserve the known emulator count limitation without inventing totals or skipping sources reported as zero.

Continue through the approved production checklist, asking only when a consequential unresolved decision or external blocker requires me. Commit completed milestones on the current branch. Do not merge, tag or publish a release. At handoff report completed slices, checks, remaining gaps and the exact next step.
```

## Production integration checkpoint — 2026-09-12

- Real App startup now creates `InvestigationWindow` and `InvestigationWorkspace`, using protected preferences, the real client factory, tolerant discovery and non-consuming message peeking. The production shell includes three paging defaults, UTC/Local, Scheduled count availability, source-aware browsing, drafts/undo, warning dialogs and the approved outlined Active badges.
- Read-only review found and integration repaired stale select-all state, grid headers behind empty states and Unicode pause/resume icons. Rendered execution found and repaired reentrant WPF shutdown. Refresh preserves entity/row references, expansion, filters, checks and focus; retained missing deliveries have an explicit tooltip.
- Current verification: Core project **91/91 passed**; App project **113/113 passed** with the legacy-migration case run separately **1/1 passed** outside the sandbox. Sandboxed Windows `File.Replace` returned `UnauthorizedAccessException`; the identical isolated synthetic-profile migration passes with escalation. Do not weaken atomic replacement to accommodate that sandbox limitation.
- Actual production Window rendering with fake broker services passes selected/unselected Active badge checks at 1500×1000, 1100×800 and 980×640. Captures live under ignored `artifacts/investigation-ui/`. This is UI evidence, not real-broker evidence. At minimum size the approved expanded-log layout may show only one full message row; the proof handles virtualization explicitly.
- Settings rendered interaction proof now passes independent page/search selectors, pending-save Done disablement, Azure CLI editing, profile switching and failed-save preservation at default/minimum sizes. The final captures were inspected; clipped delivery-budget suffix was removed because the field label already supplies the unit. Full UI gate remains **incomplete**: connection-warning dialog transitions, full end-to-end broker/UI flows, keyboard/accessibility/DPI and final parity remain outstanding.
- New emulator proof **1/1 passed** in 6 seconds: unique queue/topic/two subscriptions, 55 Active plus 5 DLQ deliveries per receiving source; real discovery/Scheduled availability; repeated combined paging without consuming; bounded search/Continue with 180 unique source-aware delivery identities. Both `sbe-investigation-proof` containers and its network were removed afterward. Ordinary local Docker projects were untouched.
- Independent Core review prompted count-free discovered descriptors, canonical Watch scope keys, pager source normalization and terminal-sequence handling, explicit empty superseded-search results, and safe HTTP auth categories. Regression tests cover these repairs. Incomplete discovery retains missing cached entities with explicitly stale count state; a complete snapshot can confirm removal.
- Global search Core supports saved cursors, Active/DLQ, Stop/Continue and approved budgets; search is not yet connected to the shell. Watch scope resolution is a foundation pending review. Watch/tray/notifications, replay and typed deletion remain unimplemented in the production shell; their buttons are temporarily disabled in this integration build. Do not call this build production complete.
- Q4 fresh-baseline versus catch-up behavior is pending a contextual user question; Q5 topic replay routing and Q6 identifiers/retries remain unanswered. Final independent source review found no remaining blocker in the repaired Core scope; independent shell repair review also passed its bounded scope. This checkpoint is a read-only integration milestone, not completion of the production checklist. No runtime lease remains active.

## Global search integration checkpoint — 2026-09-12

- The shell now runs real `DeliverySearch` through `MessageSearchWorkflow`: fresh discovery, all queue/subscription Active and DLQ sources, the approved total 30-second / 10,000-delivery default, configurable limits, Stop, Continue and explicit partial status. Incomplete discovery cannot produce a complete-namespace claim. Exhausted known sources require a new search when discovery was incomplete; interrupted discovery can be retried with Continue.
- `InvestigationSurface` preserves the browse list, checked rows, focus and session-only inspector drafts while displaying search results. Clear restores browsing and keyboard focus. Search trees show match counts, retain node identity across Continue and filter the visible matches by receiving scope. Query errors remain visible at compact/minimum sizes. Suggestions use a bounded cache of loaded observations, with separate explicit global search actions; profile/session changes invalidate cached results.
- Independent search-workflow and shell reviews prompted repairs for total-budget classification, busy/cancellation cleanup on session replacement, stable match-tree nodes, live checked counts, popup closure, group-node guards and scroll restoration/reset. The final narrow shell recheck found no remaining blocker in the reviewed paths. Core superseded-result protection remains covered by the earlier foundation tests.
- Current App verification: **134/134 passed** in the main project run, plus isolated protected legacy migration **1/1 passed** outside the sandbox (**135 total**). The earlier concurrent WPF run stalled in resource/window initialization; presentation/resource tests now share a non-parallel collection, and the full repaired run passes. Core code was unchanged in this milestone; its previous **91/91** and real-emulator leaf search proof remain prior evidence, not a new broker/UI end-to-end claim.
- Real WPF search rendering with synthetic services passes at 1500×1000, 1100×800 and 980×640: partial matches, Continue, Stop, Active/DLQ badges, scope preservation, Clear/draft restoration and invalid-query layouts. Routed keyboard suggestion selection, grouped popup content, focus after Clear, complete-empty and incomplete-discovery states are exercised. Visual inspection found clipped compact empty-state content despite window-level bounds passing; compact spacing was repaired and pane-relative heading/description/Clear-button bounds now pass. Final captures under ignored `artifacts/investigation-ui/` were inspected.
- UI gate is **PASS for the exercised search states and viewport geometry**, with **BLOCKED/missing evidence for the full-product gate**: physical keyboard/screen-reader/DPI coverage, complete broker-to-window end-to-end runs and remaining product flows are outstanding. This is a search milestone, not production completion. Watch/tray/notifications, replay and deletion remain pending. Q4 baseline behavior remains unanswered; a contextual Q5 subscription replay fan-out confirmation question is now pending; Q6 identifiers/retries remains unresolved. No Docker or other shared runtime lease remains active.

## External UI and broker verification checkpoint — 2026-09-12

- Previous goal turn was progress: global search was committed as `bef88fa`. This continuation updated the launch smoke test to the real investigation shell and added `InvestigationReadOnlyEndToEndTests` using the actual executable, UIA3, protected synthetic preferences and isolated emulator entities.
- External launch/Settings smoke **1/1 passed**. The Settings dialog can be exposed beneath its owner in the UIA tree; the test now finds the dialog's window ancestor from its stable `SettingsTabs` control. It verifies three page-size selectors and Azure CLI fields without physical mouse injection.
- The real broker-to-window case **1/1 passed** in about 26 seconds: 52 Active queue deliveries plus one DLQ target, two correlated topic subscription deliveries, persisted 25-row default, 25 → 50 → 52 paging, DLQ selection, three related results, source paths in the message grid, Clear and normal app shutdown. All **55 source/bucket/sequence/message-ID/delivery-count observations** remained equal before and after. Runtime totals were deliberately excluded as proof because emulator counts can be inaccurate.
- UIA execution exposed a real defect: toggling Active/DLQ changed the visual check but did not load the bucket because only `Click` was handled. A rendered automation-peer regression failed before the repair and passes afterward. Active/DLQ and JSON/Properties/Raw now use guarded checked-state events; current tabs cannot be accidentally deselected. Content checks verify Raw and Properties output and JSON restoration. Automation IDs now expose the count summary, search status and inspector ID; search status has polite live announcements.
- Final main App run **134/134 passed**, including the complete rendered search/settings/window collection and supported-size checks. The unchanged protected migration case retains its prior isolated **1/1** evidence. UI smoke build has zero warnings/errors. The old-shell navigation/administration/mutation smoke cases remain unmigrated; no full UI smoke suite, physical keyboard, screen-reader or DPI pass is claimed.
- Independent E2E review corrected assertions that could have matched namespace-tree labels instead of result rows and required per-run profile cleanup. Generated profile remnants from the failed diagnostic runs were removed within the test output root. The isolated `sbe-investigation-proof` containers and network were removed after the passing run; no test app/testhost or shared runtime lease remains active.
- Deletion audit is read-only so far. The existing adapter is DLQ-only and lacks per-target Active/DLQ outcomes. Arbitrary sequence-targeted receiving is documented for deferred messages, while ordinary receiving does not provide that selector ([receiver API](https://learn.microsoft.com/en-us/dotnet/api/azure.messaging.servicebus.servicebusreceiver?view=azure-dotnet)). Releasing or expiring a PeekLock increments delivery count and can move a message to DLQ ([Microsoft DLQ documentation](https://learn.microsoft.com/en-us/azure/service-bus-messaging/service-bus-dead-letter-queues)). Before implementing selected Active deletion, resolve the effects of receiving unrelated deliveries; do not ship a repeated receive/abandon loop or claim only selected deliveries are affected without a supported contract and broker proof. Q4/Q5/Q6 remain pending as recorded above.

## Connection approval verification checkpoint — 2026-09-12

- Previous milestone committed as `5791da4`; this continuation made additional verification progress. Five new workspace cases prove Cancel at either the draft prompt or destination warning preserves the current connected session, entity identity and edited document; only both approvals clear the old session. With automatic connection enabled, the destination warning is asked once and a rejected warning makes no connection attempt.
- Four actual WPF modal cases pass at 520×390 and 460×320: connection warning and unsaved-draft variants, Cancel/Continue/Discard automation invocation, initial Cancel keyboard focus, default/cancel button semantics, full profile tooltip, long multiline warning scrolling and primary-action containment. Captures under ignored `artifacts/investigation-ui/warning-*.png` were visually inspected. The default Discard case uses the shipping confirmation text.
- Corrected the warning scroll region's accessible name to follow the dialog heading, so the draft dialog no longer identifies its content as a connection warning. Layout and appearance retain the approved prototype values.
- Main App suite **143/143 passed** (the unchanged protected legacy-migration case retains its earlier isolated pass); after refining representative Discard text, the complete four-case modal matrix passed again. No broker or external UI process was needed. Coordinator owned all runtime execution; `warning_render` added only the modal test file and `warning_review` performed read-only review.
- UI gate **PASS for the exercised modal rendering, geometry, focus and automation transitions**. These component/workflow proofs do not establish a complete external profile-selector-to-dialog journey, physical keyboard routing, screen-reader announcements, DPI/multi-monitor behavior or the full-product gate. Watch, replay, deletion and their pending decisions remain outstanding.

## Production tray lifetime checkpoint — 2026-09-12

- Previous milestone committed as `4f9cdbe`. Production startup now creates a real Windows NotifyIcon with the approved Open/separator/Exit menu. Close hides the window when the saved preference is enabled and a tray exists; the connected session, drafts and ongoing work survive. Open restores a minimized/hidden window. Explicit Exit restores the owner before draft confirmation, rejects duplicate pending close requests and awaits workspace cleanup before final close. App lifetime follows the main window so future auxiliary windows cannot retain an exited application.
- Tray resource acquisition and disposal are cleanup-protected; the icon uses the existing embedded resource. A tray initialization failure logs a safe warning, leaves normal Close available, and disables the Settings switch with the approved unavailable explanation. Settings keeps the persisted preference intact so it can work in a later session with a tray.
- **3/3 tray/render cases passed**: real NotifyIcon/menu lifetime with connected synthetic services; hidden dirty draft preservation; minimized Open; repeated Exit/Close during pending approval; cancellation followed by normal hiding; accepted Exit; close-to-tray disabled; unavailable Settings at default/minimum dimensions and independent pending/completed saves. Captures `settings-tray-unavailable-*.png` under ignored `artifacts/investigation-ui/` were inspected. Native Explorer tray pixels, shell restart and physical tray-menu input are not claimed by these in-process native-object checks.
- The unavailable Settings test exposed initial ComboBox events starting an unnecessary save and ignoring the first actual page-size change. Page-size/search-budget handlers now ignore unchanged values. The regression failed with 50 instead of the requested 100 before this repair and passes afterward.
- Final main App run **146/146 passed**; protected legacy migration retains its prior isolated evidence. External executable launch/Settings/clean-close/restart proof **1/1 passed** after all repairs, confirming the close preference persists across restart. The same test hit protected preference persistence failure inside the sandbox and passed outside it; atomic storage was preserved. App/UI smoke builds report zero warnings/errors. Independent final tray review found no remaining blocker in the reviewed delta.
- UI gate **PASS for exercised tray lifetime and Settings fallback states**, with full-product accessibility/DPI/notification and broker Watch evidence still missing. No Watch polling or notifications were added: Q4 remains pending; Q5/Q6 and deletion safety remain outstanding. No broker mutation or Docker runtime was used. Tests use isolated profiles and clean their launched applications; no ordinary user data was changed.

## Broker algorithm constraints checkpoint — 2026-09-12

- Previous milestone committed as `28aa25e`. Two new isolated emulator proofs passed **2/2** in about nine seconds. No production algorithm was changed, and these tests do not establish Azure behavior.
- `InvestigationDlqArrivalIntegrationTests` sends two messages, holds both PeekLocks, dead-letters sequence 2 first, then sequence 1. Both preserve their original sequence numbers in DLQ. A full peek sees both; peeking from the previously observed maximum plus one (3) sees neither. Repeated full peeks preserve message ID, sequence and delivery count. This directly proves that a forward-only DLQ watermark misses a later arrival; Watch needs repeat scans/deduplication after the pending baseline policy is settled.
- `InvestigationActiveDeletionSafetyIntegrationTests` uses a disposable queue with maximum delivery count 1. A selected message follows an unselected one. Receiving and abandoning the unselected message moves it to DLQ with `MaxDeliveryCountExceeded`; the selected delivery remains Active with unchanged sequence/delivery count. This proves collateral effects of a receive/abandon scan, not a safe production deletion implementation. Independent review requested bounded full paging instead of assuming a single peek returns both fixtures; the repaired test passed again **1/1**.
- Official browsing documentation confirms peeking traverses sequence order and permits an explicit starting sequence, and that peeks include locked/deferred messages ([Microsoft browsing documentation](https://learn.microsoft.com/en-us/azure/service-bus-messaging/message-browsing)). The new tests supply the emulator-specific evidence above. Do not claim full rescans are transactional or guarantee observing messages that consumers remove between polls.
- Q6 was submitted with context: readable original ID plus unique replay token versus GUID-only with original metadata; ambiguous sends require inspection/manual retry rather than automatic copies. A separate consequential Active-delete question now asks whether to defer arbitrary targeting until verified safe or explicitly permit the effects of scanning unselected messages. Neither proposal is approved yet. Q4/Q5 remain pending; do not silently infer answers.
- Tests create/delete only unique queues with 120-second operation and independent 10-second cleanup bounds. Coordinator exclusively ran the dedicated `sbe-investigation-proof` project and removed its two containers and network afterward; ordinary Docker services and user messages were untouched. `dlq_cursor_proof` owned only the DLQ test, and `broker_proof_review` reviewed both tests read-only. No runtime lease remains active. No UI changed; prior UI evidence remains unchanged.

## Pending startup-load shutdown repair — 2026-09-12

- Previous milestone committed as `2851f0c`. A deterministic delayed-preference-store reproduction at the public workspace seam confirmed two defects: window-bound saving and disposal wrote startup defaults before saved profiles loaded (two unexpected saves), then the late load replaced preferences on the disposed workspace. The connection factory was never needed to reproduce either failure, separating this from a stale broker-connect result.
- `DisposeWhilePreferencesLoadIsPending_DoesNotOverwriteStoreOrPublishLatePreferences` ran red twice with separate assertions exposing the unwanted saves and late replacement. The repair gates ordinary preference saving on successful initialization, checks disposal before loading, and serializes publication of the load result against disposal with the existing lifecycle semaphore. Broker connection remains outside that semaphore and retains its existing disposal checks.
- Focused workspace tests **11/11 passed**, followed by main App **147/147 passed** (unchanged protected migration retains prior isolated evidence). The regression checks zero saves before initialization, unchanged shutdown preferences, no late property notifications, no broker connection and a disconnected final state. No rendered layout or broker behavior changed, and no user preference file was used by the reproduction. Watch/replay/deletion decisions and broader completion gaps remain outstanding.

## Watch inclusion foundation parity — 2026-09-12

- Previous milestone committed as `201a8be`. Inspection of approved prototype `WatchRules` found that entity inclusion is independent of Active/DLQ choices; the production foundation previously represented only bucket choices. `WatchPreference` now has an optional nullable `Included` value, and the protected preferences envelope round-trips it. Existing envelopes with no field load null, preserving inherited/default inclusion.
- Resolver inclusion defaults to true, inherits connection → category → topic → leaf, and suppresses both bucket targets when false. More specific inclusion overrides remain independent of bucket settings and apply to future discovered descendants. This does not yet implement editor branch mutation, mixed-state rendering, polling or notifications; Q4 remains required before their dependent behavior.
- Nine new resolver cases ran **7 failing / 2 passing** before the resolver repair, then the full Core suite passed **100/100**. Protected round-trip proof first failed by dropping Included=false. An additional duplicate-rule fixture and independent review exposed first-entry-only serialization losing later field overrides; storage now preserves ordered valid entries, matching last-non-null resolver precedence. Final main App suite **147/147 passed**, including protected round-trip and older-envelope compatibility; legacy migration retains prior isolated evidence.
- Independent re-review passed the repaired foundation scope. `watch_inclusion_tests` owned only its new Core test file; coordinator owned production changes, persistence tests and all runtime execution. No UI, broker or user profile was changed by these proofs. Watch UI/polling, replay and deletion remain outstanding; no unanswered proposal became an approved decision.

## User decisions received — 2026-09-12

- **Q4 approved:** poll every 15 seconds and use a fresh baseline on Watch startup/reconnect, without alerts for the existing backlog. DLQ polling must account for older sequence numbers arriving later, as demonstrated by the emulator proof. This supersedes prior records that Q4 was pending.
- **Q5 approved:** confirm each subscription replay, explaining that it publishes to the parent topic and can reach other matching subscriptions. Preserve the original DLQ delivery. This supersedes prior records that Q5 was pending.
- **Q6 approved and retention clarified:** persist an incrementing attempt counter per original-message family within each saved profile, across restarts. Replay IDs include the readable attempt number and retain a unique suffix to avoid collisions across users and after counter cleanup. Uncertain send outcomes count as attempts and must not trigger automatic duplicate copies. The user requires removal of saved counter state when the original and all replay copies have been deleted from DLQ, rather than retaining unnecessary history. Successful processing cannot be inferred reliably. Cleanup must follow confirmed deletion and a complete check of relevant DLQ sources for the same family; partial scans, missing current-page rows, cancellation or source errors do not prove absence. Track family lineage explicitly rather than grouping solely by a non-unique MessageId or parsing its display suffix. If a check is incomplete, retain the counter until absence can be verified.
- **Deletion scope clarified:** the user wants ordinary consumers to handle Active messages and failed messages to become eligible for deletion from DLQ. Implement DLQ-only deletion; do not have the explorer receive, abandon or deliberately fail Active messages to manufacture that transition. This supersedes the earlier arbitrary Active-delete requirement and resolves the collateral scan-effects question. Not every unhandled message is guaranteed to reach DLQ; that remains the consumer and broker configuration's behavior.
- Q4/Q5/Q6 and deletion scope are now resolved; the remaining workflows can proceed under these decisions. This records authorization and required behavior, not an implementation or verification claim.

## Watch arrival engine milestone — 2026-09-12

- Implemented Core `DeliveryWatch`: fresh silent baselines for newly watched receiving sources and connection generations, repeat full peeks rather than forward-only DLQ polling, source/bucket/sequence delivery identities, deduplicated arrivals, per-call delivery budgets and resumable scans. Each SDK request is at most 100 deliveries; short pages do not imply exhaustion. Observed identities are retained through the current/previous scan rather than as unbounded lifetime history.
- Poll state is copied and committed only after a successful, current-generation call; cancellation or target replacement cannot publish stale results or silently consume an alert. Concurrent same-configuration polls are rejected. Source failures retain their cursor/baseline state, report safe source-specific failure text and allow other sources to progress. New configurations retain unchanged source baselines and drop removed sources.
- Independent test work exposed two implementation defects before final verification: completed small sources restarted during a budget-limited scan, preventing coherent completion; and adding a target could postpone reading an already-completed existing source. Persisted scan-round state and configuration refresh handling repaired both. Final Core suite **109/109 passed**, including nine new Watch cases, a 205-delivery fixture with 37-item short pages, cancellation rollback and a new-generation poll completing while the old canceled response remains held.
- Updated real-emulator `InvestigationDlqArrivalIntegrationTests` now runs the production Watch engine through the real message service. **1/1 passed**: sequence 2 establishes a silent baseline, later DLQ sequence 1 generates one arrival, a repeat poll emits none, and a new connection generation silently baselines both. Full before/after observations retain message IDs, sequences and delivery counts. This is emulator evidence, not Azure certification.
- This milestone implements arrival detection only. The 15-second scheduler, discovery integration, Watch editor, notification routing and rendered end-to-end flows remain required. A poll is an observation over time, not a transactional snapshot; messages removed between polls cannot be guaranteed observable. No UI or mutation workflow was enabled by this Core addition.
- Coordinator owned production changes and all runtime execution. `watch_poll_tests` owned only the new unit test file; `watch_poll_review` reviewed read-only. The isolated emulator's generated queue was deleted and both `sbe-investigation-proof` containers plus network were removed after verification. Ordinary Docker services and user data were untouched; no runtime lease remains active.

## Watch workspace lifecycle milestone — 2026-09-12

- Added `MessageWatchWorkflow` and connected it to real workspace session startup, preference changes and disconnect/disposal before broker factory disposal. It polls immediately, then waits 15 seconds between completed cycles; rule changes interrupt that wait. Disabled rules perform no broker requests. Each cycle refreshes discovery, inherits rules for newly discovered entities and retains previously known entities when discovery is incomplete. Complete discovery can remove sources; reappearing sources establish fresh silent baselines.
- Each cycle allows 10,000 scanned deliveries and a 30-second operation deadline. Independent review identified that a single large engine transaction could repeatedly roll back on timeout, and that checking cancellation after an engine commit could lose already-deduplicated arrivals. The workflow now commits batches of at most 100 deliveries and publishes committed results for the current session even if the deadline fires while the continuation is queued. Later canceled batches resume on the next cycle. Session replacement still suppresses old-session results.
- Pending arrivals are retained in the workflow for the forthcoming notification surface and cleared on session stop. Discovery/timeout and per-source failures reach the existing activity log without raw exception details. The Watch editor, visual status and persistent notification/Investigate flow are still outstanding; this does not claim end-to-end monitoring UI completion.
- Eight deterministic workflow tests cover immediate/15-second scheduling, disabled rules, incomplete versus complete discovery, future sources, rule-change baseline retention, cancellation-ignoring late discovery, timeout recovery and committed progress surviving timeout. The last regression was verified red by temporarily restoring a single large engine call, then passed with the batch repair. Final main App suite **155/155 passed**, excluding the unchanged protected legacy migration test whose prior isolated evidence remains recorded. No broker, Docker or ordinary saved profile was used for these tests.
- Independent backend re-review **PASS** after all three findings were repaired. The precise deadline-between-engine-return-and-WPF-continuation path has source review but no dedicated queued-context regression. No new rendered Watch flow was enabled or claimed verified; the full-product UI gate remains unfinished. Coordinator owned production changes and runtime execution; `watch_workflow_tests` owned only its new test file and `watch_lifecycle_review` reviewed read-only.

## Watch rule editor and controls milestone — 2026-09-12

- Added the approved Global Watch window (540×640, minimum 460×520), searchable inclusion tree, independent Active/DLQ defaults, mixed parent state, and branch choices that establish future inheritance. `WatchRuleEditor` uses the production tagged rule keys and resolver; explicit topic changes reset descendant bucket overrides, while inclusion remains a separate gate. Global bucket disable retains explicit enabled leaf choices as in the prototype. Exact topic matching prevents changes to `orders` from affecting `orders/archive`.
- Connected the main window's Watch all, entity Watch popup, Watching label/bell, Stop watching and watched-location overview to persisted per-profile rules and the polling workflow. Saves disable conflicting controls and publish only after successful persistence; rejected saves restore committed choices. Settings preserves the current Watch map rather than overwriting it from an older Settings snapshot. Profile changes close the old editor; its save is scoped to the captured profile.
- Modeless editor refresh now incorporates external entity/rule changes. Unchanged workspace notifications preserve checkbox instances, keyboard focus, collapsed branches and scroll position. Changed refreshes restore focus after the replacement checkbox is loaded. Review and rendered tests exposed stale snapshot overwrites, topic-prefix collisions, long-label overflow and premature focus restoration; all were repaired. Long entity labels use available tree width with ellipsis and a full-path tooltip, preserving the approved layout at both dialog sizes.
- Ten rule-model tests and five persistence cases cover inheritance, mixed state, future entities, duplicate-field precedence, name collisions, failed saves, wrong-profile rejection and Settings queued behind a held Watch save. Two real WPF STA/UIA scenarios exercise both dialog sizes, logical keyboard traversal, inclusion changes, search/empty/long-name states, pending/rejected saves, external updates, focus preservation and Done. The original render checks ran red on long-label overflow and the added focus check ran red before deferred restoration.
- Extended the real investigation-window proof at 1500×1000, 1100×800 and 980×640: Watch all → saved global rule → Watching state → topic popup/DLQ rule → modeless global edit preserving that topic rule → overview → Stop watching. Final main App suite **172/172 passed**, excluding the unchanged protected legacy migration case with prior isolated evidence. The final additional popup/overview capture proof **1/1 passed** after test-only changes. Synthetic browser/message services were used; no broker mutation, Docker session or ordinary profile was touched.
- UI verification **PASS for exercised editor, popup, overview and main-window rule transitions**: rendered screenshots under ignored `artifacts/investigation-ui/` were inspected, including both dialog sizes and minimum main window/popup/overview. Independent source re-review **PASS**. Full-product UI/accessibility/DPI and real-broker notification evidence remain incomplete. Persistent notifications, overview's Show notifications action and notification Investigate routing are explicitly not implemented by this milestone; replay and DLQ deletion also remain outstanding. This is not a claim of complete Watch or product parity.
- Coordinator owned production changes and runtime execution. `watch_editor_tests` owned the new rule/persistence test files, `watch_editor_render` owned the new dialog render proof, and `watch_editor_review` reviewed read-only. All runtime test handles completed; no shared runtime lease remains active.

## Persistent Watch notification and investigation milestone — 2026-09-12

- Connected production Watch arrivals to the approved separate, persistent, topmost WPF notification window. The window does not activate itself or appear in the taskbar; it remains available while the main window is hidden to tray. Active/DLQ observations are grouped by receiving source and bucket, with counts and other-location summaries. Dismiss acknowledges only the current group and never changes broker messages. The overview now offers Show notifications; disabling notification display retains pending observations for re-enabling it.
- Investigate restores the main window and adds notified cases to the applied search using a tested OR-union builder. Existing valid criteria and default ID mode are preserved; blank correlation IDs use explicit message-ID alternatives as needed. Quoting, wildcard coverage and field-aware deduplication use the production grammar. The newest matching case receives focus. Only cases found in scanned results are acknowledged; missing/expired cases remain pending, and arrivals received while the search is running are not implicitly acknowledged. Superseded searches and connection changes cannot acknowledge another session's cases.
- Ordinary disconnect now preserves pending notifications; reconnect establishes a fresh silent broker baseline while retained cases can be searched in the current connection. Profile switches, successful credential/endpoint changes (including while disconnected), and exit clear prior-namespace pending state. A failed offline credential save preserves the old profile and alerts. Disabling a watched bucket/source removes its pending notifications. These rules supersede the earlier lifecycle milestone's unconditional clear-on-stop implementation note.
- Watch discovery also updates the visible entity tree for newly discovered sources. Raw partial discovery is published to Browse so missing entity counts become stale; a merged snapshot is retained only for Watch target continuity. A baseline-ready activity entry exposes when a silent initial scan has completed. Notification theme updates follow profile accent changes. Positioning uses the main window's monitor work area and physical-pixel window coordinates; available-monitor containment was exercised, but mixed-DPI and multiple-monitor hardware coverage remains missing. Long source/profile text scrolls inside a bounded area without hiding actions.
- Final main App suite **192/192 passed**, excluding the unchanged protected legacy migration case with prior isolated evidence. Added query cases, six lifecycle/persistence cases and a real STA notification render case. The render case observes persistence for 16 seconds, exercises UIA actions and long content, and checks passive-window flags and monitor containment. The main-window STA flow proves available-case acknowledgement alongside unavailable-case retention, hidden-window restoration, disconnect/reconnect, theme refresh, notification off/on, and Dismiss. A test initially asserted immediately after an asynchronously dispatched UIA Invoke; it was corrected to wait for the actual resulting log/state.
- New external-app `InvestigationWatchEndToEndTests` **1/1 passed in 45 seconds** against the real isolated emulator: existing Active/DLQ fixtures, persisted Watch rules, observed silent baseline readiness, real close-to-tray hide, two broker arrivals, notification persistence across another polling interval without recounting, Investigate restoring the application, both cases found, newest message focused and notification acknowledged. Full Active/DLQ before/after tuples retain MessageId, SequenceNumber and DeliveryCount. The later offline-credential/theme repairs were covered by the final STA/unit suite; this broker run is emulator evidence, not Azure certification.
- UI gate **PASS for exercised notification, recovery and investigation states** with inspected normal/long-content captures under ignored `artifacts/investigation-ui/`. Independent final source review **PASS** after repairing partial-discovery count truth, ordinary disconnect retention, offline credential isolation and live theme refresh. Full-product accessibility/DPI, complete mutation workflows and broader final parity checks remain outstanding; this does not close the production completion checklist.
- Coordinator owned production changes and all runtime execution. Test agents owned only the new query/lifecycle, notification render and broker-to-window proof files; source review was read-only. The external test removed its unique queue and isolated profile and terminated its launched application. Both `sbe-investigation-proof` containers and network were removed after proof completion; ordinary Docker services and user data were untouched. No runtime handles remain active.

## Replay reservation and persistence milestone — 2026-09-12

- Implemented explicit replay families and incremental attempts scoped to saved profile IDs. Original identity includes receiving source, sequence, enqueue time, message ID and raw-body hash; message IDs and readable replay suffixes are never used alone to group families. Replay copies carry explicit lineage. Deterministic original-family IDs permit independent reservations to recognize the same original, while every send ID has a fresh unique suffix and a readable attempt number within the broker's 128-character limit.
- Family state is protected with the same Windows-user protection used by saved credentials. Older envelopes without replay state remain compatible. Validation rejects invalid or duplicate family identities; Settings preserves reservations committed after its snapshot opened. Removed profiles are omitted from persisted family state.
- The workspace reserves and saves an attempt before invoking a dedicated sender. Storage failure prevents sending; uncertain sends retain their allocated number and are never retried automatically. The dedicated SDK client sets MaxRetries to zero; this option governs service-call retries as documented by [Microsoft](https://learn.microsoft.com/en-us/dotnet/api/azure.messaging.servicebus.servicebusclientoptions.retryoptions?view=azure-dotnet). Confirmed send success survives cleanup failures. Disconnect cancels the operation; shutdown also waits for its outcome and sender cleanup.
- Replay constructs a new message directly from the peeked delivery, preserving unedited raw bytes and supported routing metadata. Edited bodies replace only the body. Queue copies target their queue; subscription copies target the parent topic. No receiver is created and the original DLQ delivery is retained. Active deliveries and stale generations are rejected.
- Independent review found and repaired an inverse identity conflict that could replace a saved family with another root. Both identity dimensions are now checked and root hex casing is normalized. Final backend source re-review **PASS**. Core suite **130/130 passed**, including the conflict and casing regressions. Final main App suite **210/210 passed**, including a held-send/held-cleanup shutdown regression and excluding the unchanged protected legacy migration case with prior isolated evidence. Initial test-only issues were repaired: an SDK-invalid session/partition fixture, a read-only identity assignment and an incorrect expected storage-exception type.
- This is a backend milestone. Replay confirmation, bulk/UI orchestration, real-broker replay proof and counter cleanup are still required. Cleanup must follow confirmed DLQ deletion and complete relevant DLQ scans proving that the original and every replay copy are absent; partial, failed or canceled scans cannot clear counters. Consumer processing success is not directly observable. No mutation UI has been enabled and no new UI verification claim is made here.
- Coordinator owned all production changes and test execution; agents owned only scoped lineage, persistence and workflow test files or read-only review. No Docker runtime, ordinary broker entity or user profile was changed by this milestone's tests.

## Replay window workflow milestone — 2026-09-12

- Enabled the real inspector's Replay / Edit and Replay action using the persisted reservation and dedicated sender. Checked rows take precedence over focus. Mixed Active/DLQ batches, selected hidden drafts, invalid edited JSON and editing one message while targeting another are rejected. Unedited non-JSON/binary messages remain eligible for byte-preserving replay. A focused dirty message retains the prototype's Edit and Replay label even when invalid; the explanatory error disables its action.
- Each subscription delivery requires the existing themed confirmation dialog, explicitly naming its parent topic, possible other matching subscriptions and retention of the original. Cancel stops the remaining snapshot batch. Per-item activity distinguishes confirmed, uncertain and not-sent outcomes; batch totals also count canceled remainder. Pending sends disable repeated Replay and draft edits. Copies never remove rows or claim that the original was processed.
- Confirmed edited replay clears only the captured delivery's matching draft, including after focus moves; later edits and unrelated drafts survive. Captured profile/generation guards prevent old send results and remaining batch targets from entering a newly connected session. Counter reservations remain durable even when a late response cannot be shown in the new session's log. The send exception boundary excludes later UI updates, so a UI failure cannot relabel a confirmed send as not sent.
- Preserved prototype action placement, error/discard area, outlined 16-pixel play icon and disabled/modified states. The replay-ID hint now describes the approved persisted attempt plus unique suffix rather than promising the prototype's simulated next ID. Reused the approved warning dialog, including initial Cancel focus, for fan-out confirmation. No new standalone mutation dialog design was introduced.
- Added 22 selection/draft cases and four real WPF STA/UIA render cases. Final main App suite **236/236 passed**, excluding the unchanged protected legacy migration case with prior isolated evidence. After the final invalid-draft label parity correction, the complete four-case replay render sweep passed again. Tests exercise 1500×1000, 1100×800 and 980×640; clean, invalid, edited, pending, mixed, checked-batch, confirmed and uncertain states; focus movement during send; late response after reconnect; repeated subscription confirmation; and approval of one batch item followed by cancellation of the next. Test fixture repairs included explicit parent-topic discovery, read-only delivery construction and accessible label assertions for icon content.
- UI gate **PASS for exercised replay surface and confirmation flow**: inspected clean/invalid/pending/uncertain main-window captures and the long-content topic confirmation under ignored `artifacts/investigation-ui/replay-*.png`. Actions remain contained at supported sizes; UIA invocation and safe initial modal focus are exercised. Independent final source review **PASS**. This is synthetic-service WPF evidence; real-broker copy/fan-out proof, physical keyboard/screen-reader/DPI coverage, DLQ deletion/counter cleanup and the full-product completion audit remain outstanding.
- Coordinator owned production changes and all runtime execution. Agents owned only new selection/render test files or read-only review. Tests used isolated synthetic preferences and services; no broker, Docker runtime or ordinary profile was changed. All test handles completed.

## Replay broker proof and deletion investigation — 2026-09-12

- New `InvestigationReplayIntegrationTests` **2/2 passed in 15 seconds** against the isolated emulator. Tests use production `MessageProjection`, `ReplayLineage` and `ReplayCopySender`. Queue replay preserves invalid-UTF-8 binary bytes, content type, correlation ID, subject, ReplyTo, ReplyToSessionId, To, TTL and typed application properties. An explicit fixture consumer moves the first replay to DLQ; reserving that broker-returned copy recognizes the original family and advances to attempt 2. Original and first-copy DLQ MessageId/SequenceNumber/DeliveryCount tuples and raw bodies remain unchanged by subsequent replay.
- Parent-topic proof creates two matching subscriptions, dead-letters the original in one, then sends through the production subscription destination mapping. Both subscriptions receive the same new replay ID, and the original DLQ tuple remains unchanged. Fixture consumers explicitly complete their copies; Explorer does not consume Active messages during replay.
- Independent source review **PASS**. This is Core/emulator evidence, not Azure certification or an external-app end-to-end proof. The test does not exercise edited-body replay, session/partition routing metadata, workspace persistence or uncertain-send fault injection against the broker. Existing unit/STA evidence covers some of those paths; broader broker-to-window proof remains outstanding.
- Each test used unique queue/topic names, a 120-second cancellation bound and independent 10-second cleanup. Both fixtures were cleaned successfully. The coordinator removed both `sbe-investigation-proof` containers and its network afterward. No ordinary entity/profile was touched; no runtime handle remains active. The test agent owned only the new test file; a second agent reviewed read-only.
- Read-only deletion investigation confirms the legacy aggregate delete service must not be wired directly into this workspace: lock acquisition is outside its cleanup boundary, sequence-maximum termination can miss late lower-sequence DLQ arrivals, locks are accumulated before settlement, aggregate exception text loses exact confirmed/uncertain identities, and receiver disposal can override the result. The forthcoming DLQ-only adapter needs typed per-delivery outcomes, prompt settlement of matching captured identities, bounded scanning, and independent cleanup that preserves confirmed outcomes. Non-target DLQ locks can be touched during targeting; never promise unchanged delivery counts or complete unrelated messages.
- Cleanup integration still requires confirmed-deletion candidates, fresh complete discovery and complete beginning-to-empty DLQ peeks. A subscription family requires every current subscription of its exact parent topic as well as its original source. Missing sources/topic, partial discovery, malformed relevant lineage, failed/partial/canceled scans or failed persistence retain counters. Serialize removal with replay reservation and compare the captured profile, generation and attempt before saving. A scan observes a changing broker rather than a transactional snapshot; later copies can restore numbering from their explicit lineage. These are implementation constraints from the approved retention rule, not a completion claim.

## Selected DLQ deletion backend milestone — 2026-09-12

- Added `DlqDeliveryDeleter` and a dedicated SDK `DlqDeleteReceiver`, leaving the legacy aggregate service unchanged and the real Delete button disabled pending UI integration. The adapter accepts only distinct captured DLQ identities from one connection generation and valid queue/subscription sources. It receives from DLQ in PeekLock mode without prefetch, matches sequence plus captured source/enqueue/message-ID/raw-body fingerprint, and completes only exact matches. Dedicated SDK retries are disabled.
- Each selected identity returns Confirmed, Uncertain, Unavailable or NotAttempted. Confirmation is recorded immediately after settlement; a thrown Complete call is uncertain and is not repeated. All received locks enter the cleanup boundary before cancellation or mapping. Non-target messages remain locked until the source scan ends, avoiding repeated receive/abandon loops. No sequence-maximum early stop is used. Scans are capped at 10,000 deliveries and 30 seconds, with independent 10-second cleanup. Cleanup/disposal failures cannot erase confirmed outcomes and set an explicit incomplete flag. Unavailable means not acquired/verified, never proof that the message is absent.
- Core suite **148/148 passed**, including 18 new cases for exact matching, late high-then-low receive order, changed fingerprint, empty/error receive, settlement uncertainty, cleanup/disposal failure, budget, cancellation, source routing and pre-receive rejection. Extra regressions prove cancellation exactly as a received batch returns still releases every lock without completing anything, and a successful first settlement remains confirmed when the next settlement fails.
- Real-emulator `InvestigationDlqDeleteIntegrationTests` **2/2 passed in 20 seconds**. Queue proof captures a late lower-sequence DLQ target and a target subsequently completed by a fixture consumer; production deletion confirms the former and reports the vanished target unavailable. Complete DLQ peeks prove the unselected neighbor retains ID, sequence, body and metadata. An Active neighbor also retains its delivery count. Subscription proof exercises the topic/subscription receiver branch, deleting only its selected delivery and retaining the other DLQ message. Non-target DLQ delivery counts are intentionally not claimed unchanged because the scan acquires/releases locks.
- Independent backend and final test-source review **PASS**. These are adapter proofs, not WPF deletion/counter-cleanup or Azure/session-enabled/held-lock certification. Typed confirmation, per-item UI updates, cancellation controls, persisted pending cleanup and complete family-absence scanning remain required before the feature is complete. Existing UI verification remains unchanged because no rendered behavior was enabled by this milestone.
- Coordinator owned production changes and all runtime execution; agents owned only the new unit/integration test files or read-only review. Tests used uniquely named isolated entities with 120-second operation and independent 10-second fixture cleanup bounds; both entity fixtures were removed successfully. Both `sbe-investigation-proof` containers and its network were removed after verification. No ordinary profile/entity was touched and no runtime handle remains active.
